{
  "name": "scout-ghoster",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "dev": "concurrently \"npm start\" \" cd frontend && npm run serve\"",
    "check": "cd frontend && npm run lint",
    "heroku-postbuild": "cd frontend && npm install --only=dev && npm run build"
  },
  "dependencies": {
    "cookie-parser": "~1.4.4",
    "debug": "~2.6.9",
    "express": "~4.16.1",
    "http-errors": "~1.6.3",
    "morgan": "~1.9.1",
    "pug": "^2.0.4"
  },
  "devDependencies": {
    "concurrently": "^5.2.0"
  }
}
